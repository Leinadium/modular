Partida:
	Criar Partida:
        peao.limpar_peoes()
        peao.criar_peoes(cor)
        tabuleiro.iniciar_tabuleiro(n)

	Rodada(jogador):
		RODAR DADO: dado.rodar_dado()
		DESCOBRIR QUAIS PEOES POSSIVEIS: tabuleiro.movimentacao_possivel(pos, mov, deslocamento)
		ESCOLHER PEAO A SER JOGADO: graficos...
		MOVER PEÃO: tabuleiro.mover_peao()
        VERIFICAR PEÃO COMIDO: tabuleiro.acessar_posicao(pos)
		MOVER PEÃO COMIDO: tabuleiro.reiniciar_peao(id)

Peao:
    limpar_peoes() -> int
    criar_peao(cor) -> id
    acessar_peao(id) -> cor

Tabuleiro:
    iniciar_tabuleiro(n) -> int
    adicionar_peoes(lista_ids, lista_posicoes=None) -> int
    movimentacao_possivel(id_ mov) -> int
    mover_peao(id, mov) -> pos | int
    acessar_posicao(pos) -> lista <id> | int
    reiniciar_peao(id) -> 0

Dado:
    jogar_dado() -> int